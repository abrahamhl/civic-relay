// Executable primitive tests live in ../../tests/crypto.test.mjs (node:test).
// They import ../dist/crypto.js; run after the core package has been compiled.
// There is no end-to-end application encryption test or recovery guarantee here.
export {};
